{
  "name": "loja-checkout",
  "version": "1.0.0",
  "description": "Loja virtual com login seguro e checkout — Node + Express + Prisma + JWT",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "postinstall": "prisma generate",
    "setup": "prisma generate && prisma db push && node src/seed.js",
    "start": "node src/server.js",
    "start:prod": "prisma db push --skip-generate && node src/seed.js && node src/server.js",
    "dev": "node --watch src/server.js",
    "seed": "node src/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.4.1",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.2",
    "prisma": "^5.22.0",
    "stripe": "^17.4.0"
  }
}
